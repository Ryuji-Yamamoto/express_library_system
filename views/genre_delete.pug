extends layout

block content
  script
     | console.log(genre.name)

  h1 #{title}: #{genre.name}

  p #[Do you really want to delete this Genre?]
  
  form(method='POST' action='')
    div.form-group
      input#genre.form-control(type='hidden',name='genre', required='true', value=genre._id )

    button.btn.btn-primary(type='submit') Delete